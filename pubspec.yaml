name: discount_buddy
description: "DiscountBuddy - Your discount companion for restaurants, cafes, and more."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+5

environment:
  sdk: ^3.10.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  google_maps_flutter: ^2.9.0
  cached_network_image: ^3.3.1
  intl: ^0.20.2
  flutter_svg: ^2.0.10+1
  flutter_secure_storage: ^10.0.0
  shared_preferences: ^2.2.2
  google_sign_in: ^7.2.0
  share_plus: ^12.0.1
  mapbox_maps_flutter: ^2.6.0
  flutter_launcher_icons: ^0.14.4
  
  # Firebase for push notifications
  firebase_core: ^4.5.0
  firebase_messaging: ^16.1.1
  
  # QR Code scanning
  mobile_scanner: ^7.2.0
  permission_handler: ^12.0.1
  image_picker: ^1.1.2
  http_parser: ^4.1.2
  provider: ^6.1.1
  firebase_auth: ^6.2.0
  flutter_dotenv: ^6.0.0
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.0
  sign_in_with_apple: ^6.1.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^2.4.6
  flutter_lints: ^6.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/png/db_logo.png"
  min_sdk_android: 21
  remove_alpha_ios: true

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/png/
    - assets/svg/
    - .env
    - assets/fonts/PolySans/
    - assets/fonts/HubotSans/
    - assets/fonts/

  fonts:
    # PolySans font families
    - family: PolySans-Bulky
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-bulky.otf

    - family: PolySans-BulkyWide
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-bulkywide.otf

    - family: PolySans-Median
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-median.otf

    - family: PolySans-MedianWide
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-medianwide.otf

    - family: PolySans-Neutral
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-neutral.otf

    - family: PolySans-NeutralWide
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-neutralwide.otf

    - family: PolySans-Slim
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-slim.otf

    - family: PolySans-SlimWide
      fonts:
        - asset: assets/fonts/PolySans/polysanstrial-slimwide.otf

    # HubotSans font families
    - family: HubotSans
      fonts:
        - asset: assets/fonts/HubotSans/HubotSans-Regular.ttf
        - asset: assets/fonts/HubotSans/HubotSans-Bold.ttf
          weight: 700
        - asset: assets/fonts/HubotSans/HubotSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/HubotSans/HubotSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/HubotSans/HubotSans-Light.ttf
          weight: 300
        - asset: assets/fonts/HubotSans/HubotSans-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/HubotSans/HubotSans-Black.ttf
          weight: 900
        - asset: assets/fonts/HubotSans/HubotSans-ExtraBold.ttf
          weight: 800

    - family: HubotSans-Italic
      fonts:
        - asset: assets/fonts/HubotSans/HubotSans-Italic.ttf
        - asset: assets/fonts/HubotSans/HubotSans-BoldItalic.ttf
          weight: 700


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_native_splash:
  color: "#FFFFFF"
  image: "assets/png/db_logo.png"
  android: true
  android_gravity: center
  android_12:
    image: "assets/png/db_logo.png"
    icon_background_color: "#FFFFFF"
  ios: true
  ios_content_mode: center
